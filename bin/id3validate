#!/usr/bin/env python

import sys

from id3stuff.analyzer import Analyzer

analyzer = Analyzer()

if len(sys.argv) < 2:
    analyzer.analyze_recursively()
else:
    analyzer.analyze_recursively(sys.argv[1])
